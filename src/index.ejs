<!DOCTYPE html>
<html>

<head>
  <% for (var chunk in htmlWebpackPlugin.files.css) { %>
    <link rel="preload" href="<%= htmlWebpackPlugin.files.css[chunk] %>" as="style">
    <% } %>
      <% for (var chunk in htmlWebpackPlugin.files.chunks) { %>
        <link rel="preload" href="<%= htmlWebpackPlugin.files.chunks[chunk].entry %>" as="script">
        <% } %>
          <meta charset="utf-8">
          <title>阿京妈直销平台</title>
          <meta name="viewport" content="width=device-width, initial-scale=1.0, minimal-ui">
          <meta name="description" content="preact-boilerplate">
          <meta name="msapplication-TileColor" content="#673ab8">
          <meta name="msapplication-TileImage" content="./assets/icons/mstile-150x150.png">
          <meta name="apple-mobile-web-app-capable" content="yes">
          <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
          <meta name="apple-mobile-web-app-title" content="preact-boilerplate">
          <meta name="application-name" content="preact-boilerplate">
          <meta name="format-detection" content="telephone=no">
          <meta name="theme-color" content="#673ab8">
          <link rel="apple-touch-icon" sizes="180x180" href="./assets/icons/apple-touch-icon.png">
          <link rel="icon" type="image/png" href="/assets/icons/favicon-32x32.png" sizes="32x32">
          <link rel="icon" type="image/png" href="/assets/icons/favicon-16x16.png" sizes="16x16">
          <link rel="manifest" href="./manifest.json">
</head>

<body>
  <script>
    (function setViewPortScale(_scale) {
      window._setViewPortScale = setViewPortScale;
      var scale = _scale || (1 / (window.devicePixelRatio || 1));
      var metaEl = document.querySelector('meta[name="viewport"]');
      metaEl.setAttribute('content', 'width=device-width, initial-scale=' + scale + ', maximum-scale=' + scale + ', minimum-scale=' + scale + ', user-scalable=no');
      setBaseFont();
      window.addEventListener('resize', setBaseFont);
      function setBaseFont() {
        var docEle = document.documentElement;
        var limitWidth = 420;
        var clientWidth = docEle.clientWidth;
        window._baseFontSize = (clientWidth * scale > limitWidth ? limitWidth : clientWidth * scale) / 750 * 40;
        docEle.style.fontSize = (_baseFontSize / scale) + 'px'; // iphone6 : 1rem => 20px;
      }
      window.onclose = function () { window.onerror = null };
    })();
  </script>
  <style>
    html,
    body {
      width: 100%;
      height: 100%;
      padding: 0;
      margin: 0;
      overflow: hidden;
    }

    * {
      box-sizing: border-box;
    }
    html * ,body * {
      font-size:0.7rem;
    }

    .page_init_loading {
      width: 100%;
      height: 100%;
      z-index: 1000;
      position: fixed;
    }

    .loadingGif {
      width: 100%;
      height: 100%;
      display: block;
      background: url("") no-repeat center;
      background-size: 40%;
    }

    .fadeOut>div {
      opacity: 0;
      transition: opacity .3s;
      -webkit-transition: opacity .3s;
    }
  </style>
  <div class="page_init_loading" ontouchmove="event.preventDefault()">
    <div class="loadingGif"></div>
  </div>
  <div class='bodyHookAnchor'></div>
</body>

</html>
